{
  "brackets": [
    ["###[[", "]]###"],
    ["##[[", "]]##"],
    ["#[[", "]]#"],
    ["[[", "]]"],
    ["###[", "]###"],
    ["##[", "]##"],
    ["#[", "]#"],
    ["[", "]"],
    ["###{", "}###"],
    ["##{", "}##"],
    ["#{", "}#"],
    ["{", "}"],
    ["(", ")"]
  ],
  "colorizedBracketPairs": [],
  // The baseline set, in force until the extension activates. From then on the
  // client re-registers "autoClosingPairs" (and only that field) on every cursor
  // move with the set that suits whatever construct the caret is in, so the
  // markup delimiters below stop auto-closing in raw function arguments,
  // declaration blocks, code blocks and the like. See client/extension.js and
  // server/context.mjs. The other fields here, "surroundingPairs" included,
  // aren't swappable through that API and always come from this file.
  "autoClosingPairs": [
    { "open": "[", "close": "]" },
    { "open": "(", "close": ")" },
    { "open": "{", "close": "}" },
    { "open": "`", "close": "`" },
    { "open": "$", "close": "$" },
    { "open": "*", "close": "*" },
    { "open": "_", "close": "_" }
  ],
  "surroundingPairs": [
    ["[", "]"],
    ["(", ")"],
    ["{", "}"],
    ["`", "`"],
    ["$", "$"],
    ["*", "*"],
    ["_", "_"]
  ],
  // Folding region markers double as the source for the minimap's section
  // headers (editor.minimap.showRegionSectionHeaders, on by default), the same
  // way C# surfaces #region directives. VSCode matches the "start" regex on each
  // line and renders the text *after* the match as the header, so this regex
  // consumes the leading whitespace + 1-6 hashes + trailing whitespace of a
  // Spruce heading and leaves the heading text as the label. There is no closing
  // token for a heading, so "end" is a never-match: headings produce minimap
  // section headers without creating marker-based folds.
  "folding": {
    "markers": {
      "start": "^[ \\t]*#{1,6}[ \\t]+",
      "end": "(?!)"
    }
  }
}
